import React from 'react';
import { Modal, View, Text, TouchableOpacity, StyleSheet } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useTheme } from '@/hooks/use-theme';
import { Spacing, Radius, FontSize, FontWeight } from '@/constants/theme';
export default function DeliverySheet({ visible, onClose, onShare, onCopy }: { visible: boolean; onClose: () => void; onShare: () => void; onCopy: () => void }) { const theme = useTheme(); const action = (icon: keyof typeof Ionicons.glyphMap, label: string, press: () => void) => <TouchableOpacity style={[styles.action, { borderColor: theme.border }]} onPress={press} accessibilityLabel={label}><Ionicons name={icon} size={22} color={theme.primary} /><Text style={[styles.actionText, { color: theme.text }]}>{label}</Text></TouchableOpacity>; return <Modal visible={visible} animationType="slide" transparent onRequestClose={onClose}><View style={[styles.backdrop, { backgroundColor: theme.overlay }]}><View style={[styles.sheet, { backgroundColor: theme.surface }]}><Text style={[styles.title, { color: theme.text }]}>Share purchase order</Text>{action('share-outline', 'Share PDF', onShare)}{action('copy-outline', 'Copy order summary', onCopy)}<TouchableOpacity onPress={onClose} style={styles.cancel}><Text style={{ color: theme.textSecondary }}>Not now</Text></TouchableOpacity></View></View></Modal>; }
const styles = StyleSheet.create({ backdrop: { flex: 1, justifyContent: 'flex-end' }, sheet: { borderTopLeftRadius: Radius.lg, borderTopRightRadius: Radius.lg, padding: Spacing.four, gap: Spacing.three }, title: { fontSize: FontSize.lg, fontWeight: FontWeight.bold }, action: { minHeight: 52, borderWidth: StyleSheet.hairlineWidth, borderRadius: Radius.md, paddingHorizontal: Spacing.four, flexDirection: 'row', alignItems: 'center', gap: Spacing.three }, actionText: { fontSize: FontSize.base, fontWeight: FontWeight.semibold }, cancel: { alignItems: 'center', padding: Spacing.three } });
