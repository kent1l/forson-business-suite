{
	"name": "forson-business-suite",
	"version": "2.0.0",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"postinstall": "patch-package",
		"test": "npm run --workspaces --if-present test",
		"test:api": "npm run -w packages/api test",
		"test:web": "npm run -w packages/web test",
		"test:mobile": "npm run -w packages/mobile test",
		"lint": "npm run --workspaces --if-present lint"
	},
	"dependencies": {
		"date-fns": "^4.1.0",
		"date-fns-tz": "^3.2.0",
		"globals": "^17.6.0"
	},
	"devDependencies": {
		"patch-package": "^8.0.1"
	}
}
